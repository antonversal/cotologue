<%= simple_form_for(@value) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.association :filter %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
